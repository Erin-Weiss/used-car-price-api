# App settings
APP_NAME=Used Car Price API
APP_VERSION=0.1.0
ENVIRONMENT=dev
DEBUG=false
API_PREFIX=/api/v1
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Model / artifact paths (defaults work for local dev)
MODEL_DIR=
API_ARTIFACTS_DIR=